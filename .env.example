# WindSound usa InnerTube por padrao, sem chave manual obrigatoria.
# Deixe este arquivo aqui apenas como referencia para futuras flags locais.
VITE_APP_MODE=desktop
# Opcional: sobrescreve os valores descobertos em runtime pelo Electron.
# WINDSOUND_INNERTUBE_KEY=
# WINDSOUND_INNERTUBE_CLIENT_VERSION=
